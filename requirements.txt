# Minimal dependencies for the Vercel Python /api/chat Function.
# Keep the Streamlit/modeling environment in requirements-dashboard.txt so
# serverless bundles do not include XGBoost, PyArrow, or Streamlit.
numpy>=1.24
openai>=1.30
faiss-cpu>=1.8
python-dotenv>=1.0
